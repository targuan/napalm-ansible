---
- hosts: all
  connection: local
  tasks:
    - napalm_get_facts:
        dev_os: dummy
        ignore_notimplemented: true
        args:
          empty:
        optional_args:
          port: 8000
      register: facts
    - debug:
        var: facts
